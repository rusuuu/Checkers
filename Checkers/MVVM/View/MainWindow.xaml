<Window x:Class="Checkers.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Checkers" xmlns:viewModel="clr-namespace:Checkers.MVVM"
        mc:Ignorable="d"
        Title="Checkers" Width="600" SizeToContent="Height"
        Icon="Resources/CheckersLogo.png"
        WindowStartupLocation="CenterScreen"
        Background="#64787C"
        KeyDown="Window_KeyDown">

    <Viewbox>
        <Grid x:Name="BoardGrid"
              Height="600" Width="600"
              MouseDown="BoardGrid_MouseDown">
            <Grid.Background>
                <ImageBrush ImageSource="Resources/Board.png"/>
            </Grid.Background>

            <UniformGrid x:Name="HighlightGrid"
                         Rows="8"
                         Columns="8"
                         RenderOptions.BitmapScalingMode="HighQuality">

            </UniformGrid>

            <UniformGrid x:Name="PieceGrid"
                         Rows="8"
                         Columns="8"
                         RenderOptions.BitmapScalingMode="HighQuality">
                
            </UniformGrid>

            <ContentControl x:Name="MenuContainer"/>
        </Grid>
    </Viewbox>
</Window>
